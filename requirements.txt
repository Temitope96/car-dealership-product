# PySpark itself is already baked into the base Docker image
# (quay.io/jupyter/pyspark-notebook, Spark 4.1.0 + Java 17) — do not add
# pyspark here, it would fight the version already installed in the image.

# Delta Lake — 4.1.0 fully supports Spark 4.1.0 (the image's Spark version).
# If you change the base image's Spark version, re-check compatibility at
# https://docs.delta.io/latest/releasenotes.html before changing this pin.
delta-spark==4.1.0

# Synthetic data generation for local development (no real dealer data
# exists yet — everything in data/sample is fabricated).
faker==33.1.0

# Convenience / general use
pandas==2.2.3
python-dotenv==1.0.1
